import React from 'react';
import logo from '../../assets/logo.png';

const Logo: React.FunctionComponent = () => (
    <img className="img-fluid rounded-circle" src={logo} alt="Depiction of purple void." />
);

export default Logo;
